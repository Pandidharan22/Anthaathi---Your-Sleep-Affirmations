import { PlaceholderScreen } from '@/components/PlaceholderScreen';

export default function PlayerScreen() {
  return (
    <PlaceholderScreen
      title="Player"
      description="Loop playback and the sleep timer will live here (FR-301–FR-303)."
    />
  );
}
