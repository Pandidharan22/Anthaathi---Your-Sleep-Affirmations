// Deliberately broken file to verify the CI pipeline fails on a bad commit.
// Removed immediately after verification — see Execution Plan step 0.7.
const shouldFailTypecheck: number = 'not a number';
